const Target: React.FC = () => {
    return (
        <button className="mb-[63px] gap-1.5 py-2 px-4 shrink-0 rounded-lg bg-blue text-white text-sm">
            + Dodaj Cel
        </button>
    );
};

export default Target;
